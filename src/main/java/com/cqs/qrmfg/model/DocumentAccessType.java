package com.cqs.qrmfg.model;

/**
 * Enum representing different types of document access
 */
public enum DocumentAccessType {
    DOWNLOAD,
    VIEW,
    PREVIEW,
    METADATA_ACCESS,
    UNAUTHORIZED_ATTEMPT
}